apiVersion: v2
name: rift
description: Rift — developer tunneling platform (edge, control API, dashboard).
type: application
# Chart version; bump on template changes.
version: 0.1.0
# Default image tag when image.tag is unset.
appVersion: "0.1.0"
kubeVersion: ">=1.27.0-0"
maintainers:
  - name: Rift
home: https://rift.dev
sources:
  - https://github.com/rift/rift
